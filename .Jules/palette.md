## 2024-05-24 - Accessibility and Feature Additions
**Learning:** Screen readers often struggle with purely visual brutalist UI elements and icon-only buttons. Interactive forms also require clear labels for filtering and exporting.
**Action:** Added `aria-label`s to brutalist inputs, search buttons, copy buttons, and risk/category tags in both `App.tsx` and `DorkCard.tsx`.
